Fix repeated-label disambiguation. Do not change overlay transforms, anchorLocked behavior, or final render placement.

Problem:
Markers are now locking to tighter room-label anchors, but many signs still choose the wrong repeated occurrence of labels like UNIT 1A, UNIT 2A, UNIT 3A, etc. The active bug is candidate-instance selection, not layout.

Tasks:

When a location string matches multiple phrase candidates with similar text, do not pick the first match.
Build a candidate list for every repeated match.
Score each candidate using nearby context words within a small radius around that candidate bbox.

Use context from the full location string, such as:

unit number
nearby room number like 307B, 314B, B405
nearby room type like ELEC, MECH, TENANT STOR
neighboring labels from the same cluster
Prefer the candidate whose local neighborhood best matches the full location string.
If the location string includes a unique room number like B405, that number must dominate the score and outweigh generic matches like MECH.
If multiple candidates still tie, do not place the marker unless one candidate clearly wins.
Keep anchorLocked mode on for testing.

Debug requirements:

show the top 3 candidate phrases for each locked marker
log their:
text
bbox
local-neighborhood words
final score
visually highlight the chosen candidate in green and rejected candidates in yellow

Important:

do not use first match
do not use page-order only
do not re-enable spreading or dashed-row alignment
do not change the active render path

Success criteria:

repeated labels on the same page no longer collapse to the wrong instance
markers for locations with unique identifiers like B405 or 307B choose the correct local cluster
ambiguous signs with no clear winning candidate produce no marker
Why this is the right next step

On these multifamily plans:

UNIT 1A may appear many times
UNIT 2A may appear many times
the real differentiator is often the nearby room number or neighboring label cluster

So matching should behave like:

"MECH B405" → not just any MECH
"UNIT 1A 307B" → not just any UNIT 1A

It should choose the candidate where those terms occur together locally.