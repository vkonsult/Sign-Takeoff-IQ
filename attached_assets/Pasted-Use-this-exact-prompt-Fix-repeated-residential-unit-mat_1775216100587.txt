Use this exact prompt:

Fix repeated residential unit matching by requiring a local cluster match between the unit type and the apartment/room number.

The current system is still wrong because it matches repeated labels like UNIT 1A across the page without proving that the specific apartment number is nearby.

Do not change rendering, anchor lock, overlay transforms, or placement layout.
Do not reintroduce spreading.
Fix only the candidate selection logic.

Task:

Parse each sign location into two parts when possible:
unit type token, for example: UNIT 1A
apartment/room number token, for example: 325A
Search the page for candidates for each token separately:
all UNIT 1A candidates
all 325A candidates
Build local candidate clusters:
a valid cluster must contain both tokens within a small spatial radius
do not accept a page-wide best fuzzy match
do not accept a unit-type-only match when the number is missing
Scoring priority:
highest priority: exact apartment/room number match
second: nearby exact unit type match
third: local neighborhood support from nearby words
lowest: fuzzy similarity
Acceptance rule:
only place a marker if one local cluster clearly wins
if 325A is not found near any UNIT 1A, place no marker
if multiple clusters tie, place no marker
Anchor choice:
anchor to the apartment/room number bbox or the tight bbox around the combined cluster
do not anchor to a generic repeated UNIT 1A label alone
Keep debug / anchorLocked mode enabled

Debug requirements:

for each tested sign, log:
parsed unit token
parsed number token
all candidates for each
distance between paired candidates
chosen cluster and score
visually highlight:
accepted cluster in green
rejected clusters in yellow

Success criteria:

UNIT 1A 325A only matches where UNIT 1A and 325A occur together locally
repeated UNIT 1A labels elsewhere are rejected
ambiguous matches produce no marker
markers no longer scatter across repeated unit labels

Important:

do not use first match
do not use page-order fallback
do not broaden fuzzy matching
a missing marker is better than a wrong marker